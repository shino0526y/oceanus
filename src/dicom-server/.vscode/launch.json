{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug dicom-server",
      "preLaunchTask": "rust: cargo build",
      "program": "${workspaceFolder}/../target/debug/dicom-server",
      "args": ["ANY-SCP", "--log-level", "debug"],
      "cwd": "${workspaceFolder}",
      "sourceLanguages": ["rust"]
    }
  ]
}
